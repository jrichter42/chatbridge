# chatbridge by jrichter

import logging
from enum import Enum

logger = logging.getLogger('chatbridge')

"""
class MessageType(Enum):
    Common = 1
    Signal = 2
    Discord = 3

class Mesage:
     def __init__(self, type, id, sender, target, content, attachment, reactions):
         self.type = type
         self.id = id
         self.sender = sender
         self.target = target
         self.content = content
         self.attachment = attachment
         self.reactions = reactions
"""